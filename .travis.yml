language: php

sudo: false

php:
  - 5.3
  - 5.4
  - 5.5
  - 5.6
  - 7.0
  - hhvm

matrix:
  allow_failures:
    - php: 7.0
    - php: hhvm

script: phpunit --coverage-clover=coverage.xml tests/ 

after_success:
    - bash <(curl -s https://codecov.io/bash)